/**
 * Theme System - Central Export
 * 
 * Import from this file to access the shared theme configuration
 */

// Shared theme constants used across all components
export { theme, type SharedTheme } from './theme';

/**
 * Usage example:
 * 
 * Import the theme directly in your components:
 *    import { theme } from '../../theme/theme';
 */
